# conf/TFs_pipeline.yaml
defaults:
  - bo: hc_hebo
  - _self_

seed_num: 42

paths:
  fastq: null  # must be provided, format is standard fastq file
  label: null  # must be provided
  model: null  # must be provided, trained InstructNA model checkpoint
  output: TF_pipeline_outputs
