from setuptools import setup, find_packages

setup(
    name='InstructNA_frameworks',
    version='0.1',
    packages=find_packages(),
    install_requires=[
        # 你依赖的库，比如 'numpy', 'pandas' 等
    ],
)
